ktor {
      deployment {
           port = 8080
           port = ${?PORT}

           host = 0.0.0.0
      }

       application {
           modules = [com.example.ApplicationKt.module]
       }

         database {
             host = ${PGHOST}
             port = ${?PGPORT}
             name = ${?PGDATABASE}
             user = ${?PGUSER}
             password = ${?PGPASSWORD}
             driver = ${?DATABASE_DRIVER}
         }
}

